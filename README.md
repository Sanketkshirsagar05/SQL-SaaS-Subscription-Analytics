# 📊 SaaS Subscription Analytics (SQL Project)

## 📌 Overview
This project simulates a **real-world SaaS subscription analytics system** using **SQL Server**.  
It demonstrates **database design, analytics, performance tuning, and interview-level SQL queries**.

---

## 🧱 Database Design
Key tables:
- `users`, `plans`, `subscriptions`
- `payments`, `cancellations`
- `plan_changes`, `usage_logs`, `support_tickets`

All tables are linked using **foreign keys** to ensure data integrity.

---

## 📁 Project Structure
